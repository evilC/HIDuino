Makes the Arduino emulate a HID joystick  
Send `u` or `d` via serial to press button 2 on the joystick  
A button on the Arduino can optionally be used to trigger button 1, but no wiring is required  
